import { CardBrandsPipe } from './card-brands.pipe';

describe('CardBrandsPipe', () => {
  it('create an instance', () => {
    const pipe = new CardBrandsPipe();
    expect(pipe).toBeTruthy();
  });
});
