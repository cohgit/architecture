import { FeelingColorsPipe } from './feeling-colors.pipe';

describe('FeelingColorsPipe', () => {
  it('create an instance', () => {
    const pipe = new FeelingColorsPipe();
    expect(pipe).toBeTruthy();
  });
});
